version: '3.0'

expectations:
  population_size: 10000

actions:

  plot_bmi:
    run: r:latest analysis/edds_script.R
    outputs:
      moderately_sensitive:
        figure: output/plots/plot_bmi_bar.png
